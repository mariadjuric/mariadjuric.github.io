---
import { siteConfig } from "../config";
---

<section id="about" class="px-8 py-8 sm:px-12 md:px-20 md:py-10">
  <div class="mx-auto max-w-7xl">
    <div class="grid grid-cols-1 gap-8 lg:grid-cols-12 lg:gap-16">
      <div class="lg:col-span-3">
        <h2 class="mt-3 text-3xl font-bold text-white sm:text-4xl">About</h2>
        <div class="mt-4 h-1.5 w-20 rounded-full" style={`background-color: ${siteConfig.accentColor}`}></div>
      </div>
      <div class="space-y-5 lg:col-span-9">
        {siteConfig.aboutMe.map((paragraph) => (
          <p class="max-w-4xl text-lg leading-8 text-white/72">{paragraph}</p>
        ))}
        <div class="pt-3">
          <div class="flex flex-wrap gap-3">
            {siteConfig.skills.map((skill) => (
              <span class="px-4 py-2 text-sm font-light text-white/84">
                {skill}
              </span>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
